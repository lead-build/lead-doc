#include "mathlib.h"
#include <stdio.h>

int main() {
    printf("Libraries Example - Splitting Your Code\n");
    printf("Using math library from lib/mathlib/\n\n");
    printf("add(10, 5) = %d\n", add(10, 5));
    printf("multiply(10, 5) = %d\n", multiply(10, 5));
    printf("subtract(10, 5) = %d\n", subtract(10, 5));
    return 0;
}
